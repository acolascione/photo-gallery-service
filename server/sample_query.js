// postgres queries
// takes in restaurant_id  (bottom 10%)
// select * from restaurant where id = 10000000;

// insert into photos (user_id, description, date, restaurant_id, category, url) values ();

// cassandra queries
// takes in restaurant_id  (bottom 10%)
// select * from restaurant where id = 10000000;

// insert into photos (user_id, description, date, restaurant_id, category, url) values
